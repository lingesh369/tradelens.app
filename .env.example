# =============================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env for production or .env.local for local development
# Replace placeholder values with your actual configuration

# =============================================================================
# ENVIRONMENT PROTECTION - CRITICAL FOR DATABASE SAFETY
# =============================================================================
# Set to 'development' for local work, 'production' for live environment
NODE_ENV=development

# Database Protection - Prevents accidental production database operations
# Set to 'true' ONLY for local development, 'false' for production
ALLOW_DATABASE_RESET=true

# Production Safety Flag - Additional protection layer
# Set to 'false' for local development, 'true' for production
IS_PRODUCTION_ENVIRONMENT=false

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# For LOCAL DEVELOPMENT (using Supabase CLI):
# VITE_SUPABASE_URL=http://127.0.0.1:54321
# VITE_SUPABASE_ANON_KEY=sb_publishable_your_local_key_here
# SUPABASE_SERVICE_ROLE_KEY=sb_secret_your_local_service_key_here

# For REMOTE/PRODUCTION:
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Enable/disable analytics features
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Enable/disable real-time features
NEXT_PUBLIC_ENABLE_REAL_TIME=true

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================
# LOCAL DEVELOPMENT (.env.local):
# 1. Copy this file to .env.local
# 2. Set NODE_ENV=development, ALLOW_DATABASE_RESET=true, IS_PRODUCTION_ENVIRONMENT=false
# 3. Run 'supabase start' to get local Supabase URL and keys
# 4. Update VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY with local values

# PRODUCTION (.env):
# 1. Copy this file to .env
# 2. Set NODE_ENV=production, ALLOW_DATABASE_RESET=false, IS_PRODUCTION_ENVIRONMENT=true
# 3. Update with your actual Supabase project URL and keys from Supabase dashboard
# 4. Set environment variables in your hosting platform (Vercel, Netlify, etc.)

# SECURITY NOTES:
# - Never commit actual API keys to version control
# - Use environment variables for all sensitive information
# - Service role key should NEVER be used in client-side code
# - .env.local should be in .gitignore for local development