# Supabase Configuration
# Copy this file to .env and replace with your actual values

# Environment Protection - CRITICAL FOR DATABASE SAFETY
# Set to 'development' for local work, 'production' for live environment
NODE_ENV=development

# Database Protection - Prevents accidental production database operations
# Set to 'true' ONLY for local development, 'false' for production
ALLOW_DATABASE_RESET=true

# Production Safety Flag - Additional protection layer
# Set to 'false' for local development, 'true' for production
IS_PRODUCTION_ENVIRONMENT=false

# Supabase Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (public key, safe for client-side use)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your-project-id' with your actual Supabase project ID
# 3. Replace 'your-anon-key-here' with your actual Supabase anonymous key
# 4. Set environment protection variables correctly:
#    - For LOCAL DEVELOPMENT: NODE_ENV=development, ALLOW_DATABASE_RESET=true, IS_PRODUCTION_ENVIRONMENT=false
#    - For PRODUCTION: NODE_ENV=production, ALLOW_DATABASE_RESET=false, IS_PRODUCTION_ENVIRONMENT=true
# 5. Never commit the .env file to version control

# For production deployment:
# Set these as environment variables in your hosting platform (Vercel, Netlify, etc.)
# CRITICAL: Ensure ALLOW_DATABASE_RESET=false and IS_PRODUCTION_ENVIRONMENT=true in production